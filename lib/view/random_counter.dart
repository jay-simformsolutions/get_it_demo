import 'package:flutter/material.dart';

class RandomCounterDemo extends StatelessWidget {
  const RandomCounterDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Temp Counter Demo'),
      ),
      body: const Center(
        child: Text('Ak'),
      ),
    );
  }
}
